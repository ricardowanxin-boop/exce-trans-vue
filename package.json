{
  "name": "excel-trans-vue-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "preview": "npm --prefix frontend run preview"
  }
}
